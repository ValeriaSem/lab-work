#include <stdio.h>

int main() {
    float price;
    scanf("%f", &price);
    
    for (float i = 1.2; i <= 2.0; i += 0.2) {
        printf("%.1f кг: %.2f руб\n", i, i * price);
    }
    
    return 0;
}
